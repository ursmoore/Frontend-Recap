export const selectAllSpaces = (reduxState) => reduxState.space.spaces;
export const selectSpaceDetails = (reduxState) => reduxState.space.spaceDetails;
// export const selectUserSpace = (reduxState) => reduxState.user.space;
